using UnityEngine;
using System.Collections;

public class Max : MonoBehaviour {

	// NPC controller associated with the Max
	private NPC npc;
	public GameObject toy;
	/*
	// Use this for initialization
	void Start () {
		// Get the associated NPC controller
		npc = this.gameObject.GetComponent<NPC>();
		toy.SetActive(false);
	}
	
	public void OnDialogueComplete() {
		toy.SetActive (true);
		npc.conversationTree.ShowNextDialogue();
		Player.instance.ResetState();
	}
	*/
}
